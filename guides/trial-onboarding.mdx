---
title: "Trial onboarding"
description: "POST /v1/trial/init — the streak + dormancy model, conversion to paid."
---

<Note>
**TODO (Phase 2).** Will cover: how to mint trial sessions for
prospects, the per-key `trial_pool_limit`, the streak counter (max 10
outbound without inbound), the dormancy / reactivation flow, the
4-event trial webhook family (`trial.linked / dormant / reactivated /
converted`), and how to upsell into a rental or BYOD line.
</Note>
