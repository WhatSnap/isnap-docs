---
title: "Sending messages"
description: "POST /v1/messages — request shape, attachments, idempotency, errors, the iMessage → RCS → SMS fallback chain."
---

<Note>
**TODO (Phase 2).** Will cover: request body fields, the
`service: 'iMessage' | 'SMS' | 'RCS'` discriminator, attachments via
presigned R2 URLs, the `Idempotency-Key` contract, the full error code
catalog, and the per-line fallback chain config (`iMessage → RCS → SMS`).
</Note>
