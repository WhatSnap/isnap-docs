---
title: "Channels"
description: "iMessage, SMS, RCS — what they are, when to pick which, the fallback chain."
---

<Note>
**TODO (Phase 2).** Will cover: the `service: 'iMessage' | 'SMS' |
'RCS'` enum, why WhatsApp is intentionally not in scope, how RCS comes
"for free" on Android lines, the per-line fallback chain config
(`iMessage → RCS → SMS` is the default; configurable per line), and
how `POST /v1/lookup` informs channel selection.
</Note>
