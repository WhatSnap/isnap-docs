---
title: "BYOD pairing"
description: "Bring your own Mac+iPhone or Android — install the bridge, pair with an activation code, start sending."
---

<Note>
**TODO (Phase 2).** Will cover: `POST /v1/byod/pair`, the activation
code lifecycle, the bridge app install flow (macOS + Android), the
4-step setup checklist, what permissions the bridge needs, and how to
diagnose a paired-but-offline device.
</Note>
