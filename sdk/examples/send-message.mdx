---
title: "Send a message"
description: "End-to-end TypeScript example covering the happy path, attachments, and error handling."
---

<Note>
**TODO (Phase 2).** Will cover: a complete `sendMessage(to, text)`
helper with retry-safe idempotency, attachment uploads via
presigned R2 URLs, the `try / catch` over `APIError` to surface user-
facing failure codes, and how to cancel a queued message before it sends.
</Note>
