---
title: "Errors"
description: "The error class hierarchy, when to retry, when to surface to the user."
---

<Note>
**TODO (Phase 2).** Will cover: `APIError`, `APIConnectionError`,
`ServiceUnavailableError`, `RateLimitError`, `GoneError`, the auto-retry
behavior on 5xx + `503 Retry-After`, the `error.code` switch pattern,
and a decision tree for "retry vs fail vs prompt the user."
</Note>
