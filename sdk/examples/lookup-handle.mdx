---
title: "Look up a handle"
description: "Check if a phone number is iMessage-eligible before you send."
---

<Note>
**TODO (Phase 2).** Will cover: `POST /v1/lookup`, the 7-day cache
semantics, the 200-vs-202 cache-hit-vs-miss response shapes,
`Retry-After` handling on 429, and how to use the `iMessage / SMS / RCS`
hint to pick the right `service` field on `POST /v1/messages`.
</Note>
