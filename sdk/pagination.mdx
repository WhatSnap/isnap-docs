---
title: "Pagination"
description: "Cursor-based, async iterators, when to set page size."
---

<Note>
**TODO (Phase 2).** Will cover: the `for await ... of` async iterator
pattern, `next_cursor` lifecycle, how the SDK auto-paginates a
`.list()` call, when to set explicit `limit`, and the 24h cursor
stability guarantee.
</Note>
