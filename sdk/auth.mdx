---
title: "Authentication"
description: "API keys — minting, scoping, rotation, environment switching."
---

<Note>
**TODO (Phase 2).** Will cover: minting test vs live keys, scoping
keys to specific lines (reseller pattern), the `baseUrl` constructor
override for staging, key rotation procedure, what happens on a
revoked key (401 + `code: invalid_api_key`), and how scoped keys
interact with `POST /v1/lines/assign`.
</Note>
